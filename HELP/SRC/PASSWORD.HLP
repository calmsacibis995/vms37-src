1 PASSWORD
 Specifies the password associated with the user name  specified  on  a
 JOB card for a batch job submitted through the card reader.

 Format:

      $ PASSWORD   password
2 Parameters
 password

  Specifies the 1- through 31-character  password  associated  with
  the user name specified on the JOB command.
